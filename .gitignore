.omc/
.DS_Store

# Local source PDFs for textbook references (kept out of git).
korean/**/dekiru-kankokugo/*.pdf
korean/**/sejong-hangugeo-ipmun/*.pdf
korean/**/sejong-hangugeo-hoehwa/**/*.pdf
korean/**/sejong-hakdang-silyong-hangugeo/**/*.pdf
/korean/references/curricula/temo/테모-회화

# Nothing is packaged in this repo any more. Decks are authored here and
# `podo-curriculum` builds the lemonboard zip as part of a deploy, so a
# lemonboard-build/ directory anywhere under here is a leftover — ignored so an
# old one cannot be committed and quietly shipped as a stale deck.
lemonboard-build/

# Python bytecode from the authoring scripts in korean/tools/.
__pycache__/
